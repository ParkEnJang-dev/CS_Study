# [자료구조] 스택(Stack) & 큐(Queue) 완벽 정리

## 1. 스택 (Stack)

* **개념**: 한쪽 끝에서만 자료를 넣고 뺄 수 있는 **LIFO(Last In First Out, 후입선출)** 구조입니다.
* **특징**: 가장 최근에 들어온 데이터가 가장 먼저 나갑니다.
* **활용 사례**:
* 웹 브라우저 뒤로가기
* 실행 취소 (Undo)
* 재귀 알고리즘, DFS(깊이 우선 탐색)
* 수식 계산 (후위 표기법)



### 💻 Java 스택 구현 (Standard Library)

```java
import java.util.Stack;

public class StackExample {
    public static void main(String[] args) {
        Stack<Integer> stack = new Stack<>();

        // 데이터 추가 (Push)
        stack.push(10);
        stack.push(20);
        stack.push(30);

        // 가장 위 데이터 확인 (Peek) - 제거 안 함
        System.out.println("Top item: " + stack.peek()); // 30

        // 데이터 추출 (Pop) - 제거 함
        System.out.println("Popped: " + stack.pop()); // 30
        System.out.println("Next Popped: " + stack.pop()); // 20
    }
}

```

---

## 2. 큐 (Queue)

* **개념**: 한쪽 끝(Rear)에서는 삽입이, 반대쪽 끝(Front)에서는 삭제가 일어나는 **FIFO(First In First Out, 선입선출)** 구조입니다.
* **특징**: 먼저 들어온 데이터가 가장 먼저 나갑니다.
* **활용 사례**:
* 프린터 인쇄 대기열
* 은행 창구 줄 서기
* 프로세스 관리 (CPU 스케줄링)
* BFS(너비 우선 탐색)



### 💻 Java 큐 구현 (Standard Library)

Java에서 `Queue`는 인터페이스이므로 주로 `LinkedList`를 사용하여 구현합니다.

```java
import java.util.LinkedList;
import java.util.Queue;

public class QueueExample {
    public static void main(String[] args) {
        // Queue 인터페이스 구현체로 LinkedList 사용
        Queue<String> queue = new LinkedList<>();

        // 데이터 추가 (Enqueue / Offer)
        queue.offer("First");
        queue.offer("Second");
        queue.offer("Third");

        // 가장 앞의 데이터 확인 (Peek)
        System.out.println("Front item: " + queue.peek()); // First

        // 데이터 추출 (Dequeue / Poll)
        System.out.println("Polled: " + queue.poll()); // First
        System.out.println("Next Polled: " + queue.poll()); // Second
    }
}

```

---

## 3. 스택 vs 큐 비교 요약

| 구분 | 스택 (Stack) | 큐 (Queue) |
| --- | --- | --- |
| **순서** | LIFO (후입선출) | FIFO (선입선출) |
| **삽입/삭제 위치** | Top 한 곳에서 모두 발생 | Rear(삽입) / Front(삭제) 분리 |
| **Java 클래스** | `java.util.Stack` | `java.util.Queue` (Interface) |
| **주요 메서드** | `push()`, `pop()`, `peek()` | `offer()`, `poll()`, `peek()` |

---

## 4. 기술 면접 예상 질문 (Q&A)

### Q1. Stack 2개로 Queue를 구현하는 방법은?

> **A:** 하나의 스택(In-Stack)은 삽입용으로 쓰고, 다른 하나(Out-Stack)는 추출용으로 사용합니다. 추출 요청이 올 때 In-Stack의 데이터를 모두 Pop해서 Out-Stack으로 옮기면 순서가 뒤집히면서 FIFO 구조를 만들 수 있습니다.

### Q2. Array로 구현한 Queue와 LinkedList로 구현한 Queue의 차이는?

> **A:** Array 기반 큐는 크기가 고정되어 있어 꽉 찼을 때 문제가 생길 수 있고, 삭제 시 앞 공간을 채우기 위해 데이터를 옮기는 오버헤드가 발생할 수 있습니다(원형 큐로 보완 가능). 반면 LinkedList 기반 큐는 삽입/삭제가 빈번할 때 효율적이며 크기 제한이 없습니다.

### Q3. 스택을 사용하는 실제 알고리즘의 예시를 들어보세요.

> **A:** 대표적으로 **재귀 함수**의 호출 시스템 스택이 있습니다. 또한 그래프 탐색 중 **DFS(깊이 우선 탐색)**를 구현할 때 방문 경로를 저장하기 위해 스택을 사용합니다.

### Q4. Queue 인터페이스를 사용할 때 add()와 offer()의 차이는 무엇인가요?

> **A:** 둘 다 삽입을 수행하지만, 큐가 꽉 찼을 때 `add()`는 예외(`Exception`)를 발생시키고, `offer()`는 `false`를 반환합니다. 따라서 예외 처리가 중요한 시스템에서는 `offer()`를 권장합니다.

---